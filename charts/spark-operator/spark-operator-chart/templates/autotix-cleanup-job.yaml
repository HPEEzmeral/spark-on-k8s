{{ if .Values.autotix.enable }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "spark-operator.autotixCleanUpName" . }}
  annotations:
    "helm.sh/hook": pre-delete
    "helm.sh/hook-delete-policy": hook-succeeded
  {{- if .Values.ownerReference.overRide }}
  {{- with  .Values.ownerReference.ownerReferences }}
  ownerReferences: {{- toYaml . | nindent 2 }}
  {{- end }}
  {{- end }}
spec:
  template:
    metadata:
      name: {{ include "spark-operator.autotixCleanUpName" . }}
    spec:
      serviceAccountName: {{ include "autotix.serviceAccountName" . }}
      restartPolicy: OnFailure
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
      {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: pre-delete-container
        image: "{{ .Values.image.baseRepository }}/{{ .Values.image.hookName }}:{{ .Values.image.hookTag }}"
        imagePullPolicy: IfNotPresent
        command: ["/opt/autoticket-generator/autoticket-generator-uninstall.sh", "{{ .Release.Namespace }}"]
        resources:
          {{- toYaml .Values.resources | nindent 10 }}
  {{ end }}