{{ if .Values.autotix.enable }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "autoticket-generator.autotixCleanUpName" . }}
  annotations:
    "helm.sh/hook": pre-delete, pre-upgrade
    "helm.sh/hook-delete-policy": hook-succeeded
  {{- if .Values.ownerReference.overRide }}
  {{- with  .Values.ownerReference.ownerReferences }}
  ownerReferences: {{- toYaml . | nindent 2 }}
  {{- end }}
  {{- end }}
spec:
  template:
    metadata:
      name: {{ include "autoticket-generator.autotixCleanUpName" . }}
    spec:
      serviceAccountName: {{ include "autoticket-generator.serviceAccountName" . }}
      restartPolicy: OnFailure
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
      {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: pre-delete-container
        image: "{{ .Values.image.baseRepository }}/{{ .Values.image.autotixImageName }}:{{ .Values.image.autotixTag }}"
        imagePullPolicy: IfNotPresent
        command: ["/opt/autotix/deploy/autoticket-generator-uninstall.sh", "{{ .Release.Namespace }}", {{ include "autoticket-generator.autotixInitName" . }}]
        resources:
          {{- toYaml .Values.resources | nindent 10 }}
  {{ end }}
